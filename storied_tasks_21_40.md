# Audioura Release: Storied — Sprint 1 Batch 2 (Tasks 21–40)

| # | Release | Track | Owner | Task name | Description (inputs → work → outputs) | Acceptance criteria | Priority | Depends on (#) | Est. effort | Target week |
|---|---------|-------|-------|-----------|----------------------------------------|---------------------|----------|----------------|-------------|-------------|
| 21 | Storied | Development | Claude | Define story-type taxonomy JSON file | Input: `STORIED_DEVELOPMENT_PLAN.md` feature #1, `generate_tour_text.py` Phase 5 description prompt. Work: create `development/story_type_taxonomy.json` defining 6 story types: `history`, `anecdote`, `architecture`, `culture`, `nature`, `art` — each with: type name, tone_instruction (1 sentence for prompt injection), example_signal_phrases (3 phrases GPT should use), forbidden_phrases (3 overused phrases to ban). Output: `story_type_taxonomy.json` committed to `storied`. | File is valid JSON, parseable with `json.load()`. All 6 types present, each has all 4 keys, no type has empty forbidden_phrases list. `python -c "import json; d=json.load(open('story_type_taxonomy.json')); assert len(d['types'])==6"` exits 0. | P1 | — | 1 hr | Week 1 |
| 22 | Storied | Development | Claude | Write `assign_story_types(poi_list, tour_category)` in new file `story_type_assigner.py` | Input: `story_type_taxonomy.json` (#21), `generate_tour_text.py` poi_list structure. Work: write `development/story_type_assigner.py` with `assign_story_types(poi_list, tour_category)` — assigns a story_type to each POI ensuring no two consecutive stops share the same type, using round-robin with per-tour-category weighting (museum: art+history+anecdote; walking: culture+history+architecture; restaurant: anecdote+culture). Returns poi_list with `story_type` key added to each entry. No API call. Output: `story_type_assigner.py` committed. | Given 10-stop museum poi_list: no two adjacent stops have identical story_type. Given 10-stop walking poi_list: `culture` and `history` appear at least twice each. Function is deterministic (same input = same output). Runs in < 0.1s. | P1 | 21 | 1 hr | Week 1 |
| 23 | Storied | Development | Claude | Build forbidden-phrase regex list in `derepetition_guard.py` | Input: `chagall_current_tour.txt` (baseline tour showing repetition patterns), `story_type_taxonomy.json` (#21) forbidden_phrases. Work: write `development/derepetition_guard.py` — compile a master list of 20+ forbidden/overused phrases identified from the baseline tour (e.g. "vibrant colors and dreamlike imagery", "deep connection to his Jewish heritage", "intricate details", "timeless themes", "position yourself in the center") as compiled regex patterns. Export `FORBIDDEN_PHRASES` list and `scan_for_repetition(text)` → returns list of matched phrases found. Output: `derepetition_guard.py` committed. | `scan_for_repetition(open("chagall_current_tour.txt").read())` returns list with at least 8 matches. `scan_for_repetition("A completely original sentence with no clichés.")` returns empty list. | P1 | — | 2 hrs | Week 1 |
| 24 | Storied | Development | Claude | Inject story-type tone + forbidden-phrase ban into `_generate_description()` | Input: `generate_tour_text.py` after task #10, `story_type_assigner.py` (#22), `derepetition_guard.py` (#23), `story_type_taxonomy.json` (#21). Work: modify `_generate_description()` to accept optional `story_type` string. If provided: (a) look up `tone_instruction` from taxonomy and prepend to description prompt as a STYLE instruction, (b) append all `forbidden_phrases` for that type plus the master `FORBIDDEN_PHRASES` list as explicit DO NOT USE instructions. If `story_type` is None, behavior unchanged. Output: updated `generate_tour_text.py` committed. | With story_type="anecdote", generated description for Chagall Stop 1 does not contain any phrase from `scan_for_repetition()` master list. With story_type=None, output identical to pre-task behavior. | P1 | 22, 23, 10 | 2 hrs | Week 2 |
| 25 | Storied | Development | Claude | Wire `assign_story_types()` into `generate_tour_text()` under `STORIED_MODE` flag | Input: `generate_tour_text.py` after #24, `story_type_assigner.py` (#22). Work: when `STORIED_MODE=true`, call `assign_story_types(poi_list, tour_category)` after Phase 3B (poi_list is ordered and structured), then pass each POI's `story_type` into its `_generate_description()` call. When `STORIED_MODE=false`, no change. Output: updated `generate_tour_text.py` committed. | With `STORIED_MODE=true` on 10-stop Chagall tour: `story_type` key present on all 10 POIs in the assembled output log, no two consecutive stops share the same type. With `STORIED_MODE=false`: no `story_type` assignment occurs. | P1 | 24, 25 | 1 hr | Week 2 |
| 26 | Storied | Development | Claude | Write `check_cross_stop_repetition(tour_text)` in `derepetition_guard.py` | Input: `derepetition_guard.py` (#23), `chagall_current_tour.txt`. Work: add `check_cross_stop_repetition(tour_text)` — splits assembled tour into per-stop blocks, extracts all sentences, finds sentences with cosine-similarity > 0.85 (using simple word-overlap Jaccard, no external ML library) across different stops, returns list of `{stop_a, stop_b, sentence_a, sentence_b, similarity}` dicts. Output: updated `derepetition_guard.py` committed. | Given `chagall_current_tour.txt`: returns at least 3 cross-stop near-duplicate pairs (known: "position yourself in the center", "vibrant colors", "broader context of the museum" appear on multiple stops). Given a tour with all unique sentences: returns empty list. Runs in < 2s on a 10-stop tour. | P1 | 23 | 3 hrs | Week 2 |
| 27 | Storied | Development | Claude | Add post-assembly de-repetition check and log to Phase 6 in `generate_tour_text.py` | Input: `generate_tour_text.py` after #25, `derepetition_guard.py` (#26). Work: after Phase 6 assembly, when `STORIED_MODE=true`, call `check_cross_stop_repetition(complete_tour)` and log any pairs found to stdout as warnings: `REPETITION WARN: Stop N and Stop M share near-identical sentence`. Do NOT regenerate — log only at this stage (regeneration is task #28). Output: updated `generate_tour_text.py` committed. | With `STORIED_MODE=true` on Chagall baseline POI list (pre-Storied prompts), at least 2 repetition warnings appear in stdout. With `STORIED_MODE=false`, no repetition check runs. Warnings do not halt or modify tour output. | P1 | 26, 25 | 1 hr | Week 2 |
| 28 | Storied | Development | Claude | Write `rewrite_repeated_sentence(sentence, stop_name, story_type, api_key)` in `derepetition_guard.py` | Input: `derepetition_guard.py` (#27), `story_type_taxonomy.json` (#21). Work: add `rewrite_repeated_sentence(sentence, stop_name, story_type, api_key)` — calls gpt-3.5-turbo with instruction to rewrite the sentence for `stop_name` in the voice of `story_type` without using any of the forbidden phrases, keeping same factual content, target 1–2 sentences output. Returns rewritten string. Cost ~$0.001 per call. Output: updated `derepetition_guard.py` committed. | Given input sentence "Chagall's vibrant colors and dreamlike imagery", stop_name="Song of Songs Room", story_type="art": returned string does not contain "vibrant colors" or "dreamlike imagery", is 1–2 sentences, factually compatible with Chagall. Cost < $0.002. | P2 | 26, 21 | 2 hrs | Week 2 |
| 29 | Storied | Development | Claude | Wire auto-rewrite of flagged repetitions into Phase 6 assembly under `STORIED_MODE` | Input: `generate_tour_text.py` after #27, `derepetition_guard.py` (#28). Work: when `STORIED_MODE=true`, after logging repetition warnings, for each flagged pair call `rewrite_repeated_sentence()` on the second occurrence (keep first, rewrite second), replace the sentence in `complete_tour` string, log `REPETITION FIXED: Stop N sentence rewritten`. Cap rewrites at 10 per tour to bound cost. Output: updated `generate_tour_text.py` committed. | With `STORIED_MODE=true` on Chagall tour: stdout contains at least one `REPETITION FIXED` line. Final assembled tour passes `check_cross_stop_repetition()` with 0 pairs above 0.85 similarity (or reduced vs. baseline). Total added cost < $0.01. | P2 | 28, 27 | 2 hrs | Week 3 |
| 30 | Storied | Development | Claude | Fix fabricated walking directions — write `generate_real_directions(from_poi, to_poi, api_key)` in new file `directions_generator.py` | Input: `generate_tour_text.py` Phase 3B directions pattern, `chagall_current_tour.txt` showing fabricated "turn right, walk 50 meters" directions. Work: write `development/directions_generator.py` with `generate_real_directions(from_poi, to_poi, api_key)` — for MUSEUM tours, generates contextual room-to-room guidance ("exit through the archway on your left, the next room is straight ahead") grounded in the stop names and museum layout, using gpt-3.5-turbo with explicit instruction: DO NOT invent street distances or compass bearings inside a building. Returns direction string 1–3 sentences. Output: `directions_generator.py` committed. | Given from_poi="Biblical Message Room", to_poi="Tribe of Judah Room", venue="Musée National Marc Chagall": returned string contains no compass bearings (north/south/east/west), no invented meter distances, is 1–3 sentences, references the destination room name. Cost < $0.002. | P1 | — | 2 hrs | Week 2 |
| 31 | Storied | Development | Claude | Add `generate_walking_directions(from_poi, to_poi, location, api_key)` to `directions_generator.py` for outdoor walking tours | Input: `directions_generator.py` (#30), `generate_tour_text.py` Phase 3B. Work: add `generate_walking_directions()` variant for non-museum tours — uses street addresses from both POIs to generate landmark-based directions ("walk past the church on your left, turn at the green awning") rather than compass bearings; explicitly bans fabricated turn-by-turn distances; 2–4 sentences. Output: updated `directions_generator.py` committed. | Given two Boston POIs with real addresses: output is 2–4 sentences, contains no fabricated distances like "walk 50 meters", references at least one landmark or street name derivable from the addresses, cost < $0.002. | P1 | 30 | 1 hr | Week 2 |
| 32 | Storied | Development | Claude | Wire `directions_generator.py` into Phase 6 assembly under `STORIED_MODE` flag | Input: `generate_tour_text.py` after #29, `directions_generator.py` (#31). Work: when `STORIED_MODE=true`, replace the Phase 3B-sourced directions string in the "Please resume the tour at X" line with output from `generate_real_directions()` (museum) or `generate_walking_directions()` (walking/restaurant) called per-stop-transition. Run transitions in parallel with ThreadPoolExecutor(max_workers=5). When `STORIED_MODE=false`, Phase 3B directions used unchanged. Output: updated `generate_tour_text.py` committed. | With `STORIED_MODE=true` on Chagall museum tour: no stop transition contains compass bearings or fabricated meter distances. With `STORIED_MODE=true` on a walking tour: no transition contains "walk X meters". With `STORIED_MODE=false`: original Phase 3B directions unchanged. Total added time < 15s for 10 transitions. | P1 | 31, 29 | 2 hrs | Week 3 |
| 33 | Storied | Development | Claude | Complete spine template for walking tours (`spine_walking.txt`) with end-to-end test | Input: `spine_walking.txt` (#2), `spine_generator.py` (#3), `spine_quality_scorer.py` (#14). Work: refine `spine_walking.txt` — chapter_role vocabulary: arrival → orientation → discovery → hidden_gem → contrast → revelation → departure; connecting_thread guidance: neighborhood character + hidden history; closing_revelation instruction: must name one specific street, building, or person. Run `generate_spine()` for a real walking tour (e.g. "walking tour of Beacon Hill, Boston, 8 stops"), score with `spine_quality_scorer.py`. Iterate until score = 4/4. Output: finalized `spine_walking.txt` committed. | `generate_spine()` with Beacon Hill 8-stop POI list returns spine scoring 4/4 on `spine_quality_scorer.py`. closing_revelation contains a specific proper noun. No two stops share identical emotional_beat. | P1 | 2, 14 | 2 hrs | Week 2 |
| 34 | Storied | Development | Claude | Complete spine template for restaurant tours (`spine_restaurant.txt`) with end-to-end test | Input: `spine_restaurant.txt` (#2), `spine_generator.py` (#3), `spine_quality_scorer.py` (#14). Work: refine `spine_restaurant.txt` — chapter_role vocabulary: aperitivo → first_course → main → palette_cleanser → dessert → digestif; connecting_thread guidance: cuisine culture + chef/owner story arc; closing_revelation: must name one dish or culinary tradition specific to the location. Run `generate_spine()` for a real restaurant tour (e.g. "restaurant tour of North End Boston, 8 stops"), score with `spine_quality_scorer.py`. Iterate until score = 4/4. Output: finalized `spine_restaurant.txt` committed. | `generate_spine()` with North End 8-stop restaurant list returns spine scoring 4/4. closing_revelation references a specific dish or culinary tradition. chapter_roles follow culinary arc vocabulary. | P1 | 2, 14 | 2 hrs | Week 2 |
| 35 | Storied | Development | Claude | Complete spine template for book/movie tours (`spine_book.txt`) with end-to-end test | Input: `spine_book.txt` (#2), `spine_generator.py` (#3), `spine_quality_scorer.py` (#14). Work: refine `spine_book.txt` — chapter_role vocabulary mirrors story structure: inciting_incident → rising_action → midpoint_turn → dark_moment → climax → resolution → epilogue; connecting_thread guidance: character motivations + scene context; closing_revelation: must reference the book/film title and one specific scene. Run `generate_spine()` for a themed tour (e.g. "Harry Potter filming locations London, 8 stops"), score. Iterate until score = 4/4. Output: finalized `spine_book.txt` committed. | `generate_spine()` with Harry Potter 8-stop location list returns spine scoring 4/4. closing_revelation names the source material and one scene. chapter_roles follow story-structure vocabulary. | P2 | 2, 14 | 2 hrs | Week 2 |
| 36 | Storied | Development | Claude | Write `select_spine_template(tour_category)` router in `spine_generator.py` | Input: `spine_generator.py` (#3), all 4 finalized template files (#1, #33, #34, #35). Work: add `select_spine_template(tour_category)` function that maps tour_category string → correct template file path. Handles: `museum` → `spine_museum.txt`, `walking` → `spine_walking.txt`, `restaurant` → `spine_restaurant.txt`, `specialized` (book/movie) → `spine_book.txt`, unknown → `spine_walking.txt` (safe fallback). Update `generate_spine()` to call this router instead of hardcoded museum path. Output: updated `spine_generator.py` committed. | `select_spine_template("museum")` returns path ending in `spine_museum.txt`. `select_spine_template("restaurant")` returns path ending in `spine_restaurant.txt`. `select_spine_template("unknown_type")` returns path ending in `spine_walking.txt` (fallback, no exception). `generate_spine()` called with tour_category="walking" uses walking template (verified by checking that chapter_role values in output match walking vocabulary). | P1 | 33, 34, 35 | 1 hr | Week 2 |
| 37 | Storied | Development | Claude | Write `generate_tour_hook_audio(tour_hook, api_key)` stub in new file `tour_hook_generator.py` | Input: `spine_generator.py` (#3), `chagall_spine_poc.json` (tour_hook field example). Work: write `development/tour_hook_generator.py` with `generate_tour_hook_audio(tour_hook, api_key)` — takes the `tour_hook` string from the spine JSON, calls gpt-3.5-turbo to expand it into a 40–60 word spoken introduction paragraph (not a question — a compelling statement that establishes mystery or stakes), returns the paragraph string. Output: `tour_hook_generator.py` committed. | Given tour_hook "What secret did Chagall embed in his art...": returned paragraph is 40–60 words, written in second-person present tense ("You are standing..."), does not end with a question mark, contains the word "Chagall". Cost < $0.002. | P2 | 3 | 1 hr | Week 3 |
| 38 | Storied | Development | Claude | Wire tour hook intro into Phase 6 assembly under `STORIED_MODE` | Input: `generate_tour_text.py` after #32, `tour_hook_generator.py` (#37). Work: when `STORIED_MODE=true`, after spine is generated, call `generate_tour_hook_audio()` with `spine['tour_hook']` and prepend the result as a new "Introduction" block before Stop 1 in `complete_tour`. Format: `Introduction:\n\n{hook_paragraph}\n\n`. When `STORIED_MODE=false`, no introduction block added. Output: updated `generate_tour_text.py` committed. | With `STORIED_MODE=true`: assembled tour text starts with "Introduction:" before "Stop 1:". Introduction block is 40–60 words. With `STORIED_MODE=false`: tour starts directly with "Stop 1:" as before. | P2 | 37, 32 | 1 hr | Week 3 |
| 39 | Storied | Development | Claude | Write `content_qa_runner.py` — automated QA pass over a generated Storied tour | Input: `derepetition_guard.py` (#26), `chagall_current_tour.txt` as baseline, `validate_storied_tour.py` (#13). Work: write `development/content_qa_runner.py` — runs 8 automated QA checks on any tour text file: (1) no forbidden phrases from master list, (2) no cross-stop repetition pairs above 0.85, (3) all stops have distinct opening sentences, (4) no fabricated compass bearings in museum tour transitions, (5) Introduction block present, (6) closing_revelation present in final stop, (7) word count per stop between 200–500, (8) total cost logged and < $0.15. Prints PASS/FAIL per check + overall score. Output: `content_qa_runner.py` committed. | Run against `chagall_current_tour.txt` (Beta baseline): scores ≤ 4/8 (known failures). Run against a `STORIED_MODE=true` generated Chagall tour: scores ≥ 7/8. Script exits with code 0 on ≥6/8, code 1 on <6/8. | P1 | 26, 13 | 3 hrs | Week 3 |
| 40 | Storied | Development | Claude | Run content QA on all 4 tour types and document results in `storied_qa_results.md` | Input: `content_qa_runner.py` (#39), `spine_generator.py` (#36 — all 4 templates), `generate_tour_text.py` with `STORIED_MODE=true`. Work: generate one full Storied tour for each of the 4 tour categories (museum: Chagall Nice; walking: Beacon Hill Boston 8 stops; restaurant: North End Boston 8 stops; book: Harry Potter London 8 stops) with `STORIED_MODE=true`. Run `content_qa_runner.py` on each. Record scores, cost, generation time. Write `development/storied_qa_results.md` with results table. Output: `storied_qa_results.md` committed. | All 4 tours score ≥ 7/8 on `content_qa_runner.py`. Each tour's cost < $0.15. Each tour's generation time < 150s. `storied_qa_results.md` exists and contains a results table with all 4 rows. | P1 | 39, 36 | 4 hrs | Week 3 |
