# Audioura Release: Storied — Sprint 1 Batch 3 (Tasks 41–60)

| # | Release | Track | Owner | Task name | Description (inputs → work → outputs) | Acceptance criteria | Priority | Depends on (#) | Est. effort | Target week |
|---|---------|-------|-------|-----------|----------------------------------------|---------------------|----------|----------------|-------------|-------------|
| 41 | Storied | Development | Claude | Write `onboarding_preference.py` — user persona enum + storage schema | Input: `NEW_ARCHITECTURE_STORIED_SPLIT.md` onboarding section ("What brings you here?" → 4 emoji), `STORIED_DEVELOPMENT_PLAN.md` feature #3. Work: write `development/onboarding_preference.py` defining: (a) `UserPersona` enum with values `ART_LOVER`, `HISTORY_BUFF`, `FAMILY`, `FIRST_TIME_VISITOR`, (b) `persona_to_story_type_weights(persona)` function returning a dict mapping story_type names to weights (e.g. ART_LOVER → {art: 0.5, anecdote: 0.3, history: 0.2}), (c) `PERSONA_TONE_OVERRIDE` dict mapping persona → tone adjective injected into description prompts. No DB call — pure logic. Output: `onboarding_preference.py` committed. | `persona_to_story_type_weights(UserPersona.ART_LOVER)` returns dict with all 6 story_type keys summing to 1.0. `persona_to_story_type_weights(UserPersona.FAMILY)` gives `anecdote` the highest weight. All 4 personas return valid weight dicts. No external imports beyond stdlib. | P1 | 21 | 2 hrs | Week 2 |
| 42 | Storied | Development | Claude | Extend `assign_story_types()` to accept optional persona weighting | Input: `story_type_assigner.py` (#22), `onboarding_preference.py` (#41). Work: modify `assign_story_types(poi_list, tour_category, persona=None)` — if `persona` provided, use `persona_to_story_type_weights()` to bias story type selection (weighted random without consecutive repeats) rather than pure round-robin. If `persona=None`, behavior identical to task #22 implementation. Output: updated `story_type_assigner.py` committed. | With `persona=UserPersona.ART_LOVER` and 10 stops: `art` story_type appears at least 4 times (weight ~0.5). With `persona=None`: distribution is uniform round-robin as before (task #22 behavior unchanged). No consecutive repeats in either case. | P1 | 41, 22 | 1 hr | Week 2 |
| 43 | Storied | Development | Claude | Add `persona` parameter to `generate_tour_text()` and wire through pipeline under `STORIED_MODE` | Input: `generate_tour_text.py` after #32, `onboarding_preference.py` (#41), `story_type_assigner.py` (#42). Work: add optional `persona` string parameter to `generate_tour_text(location, tour_type, output_file, total_stops, persona=None)`. When `STORIED_MODE=true` and `persona` is not None: (a) parse persona string to `UserPersona` enum (case-insensitive, default to `FIRST_TIME_VISITOR` on unknown value), (b) pass to `assign_story_types()`, (c) inject `PERSONA_TONE_OVERRIDE[persona]` into each `_generate_description()` system prompt. When `STORIED_MODE=false` or `persona=None`: no change. Output: updated `generate_tour_text.py` committed. | `generate_tour_text(..., persona="art_lover")` with `STORIED_MODE=true`: all 10 stops have story_type assigned with `art` appearing ≥ 4 times. `generate_tour_text(..., persona="unknown_value")` defaults to `FIRST_TIME_VISITOR` without raising exception. `persona=None` produces identical output to no-persona run. | P1 | 42, 43 | 2 hrs | Week 2 |
| 44 | Storied | Development | Claude | Write `persona_preference_store.py` — Postgres-backed user persona persistence | Input: `onboarding_preference.py` (#41), `tour_cache_layer1.py` (#19) as Postgres pattern, existing `development-postgres-2-1` container. Work: write `development/persona_preference_store.py` with: `save_persona(user_id, persona, db_url)` (upsert into `user_preferences` table), `get_persona(user_id, db_url)` (returns `UserPersona` or None). Creates table `user_preferences(user_id TEXT PRIMARY KEY, persona TEXT, updated_at TIMESTAMP)` if not exists. Output: `persona_preference_store.py` committed. | `save_persona("test_user_1", UserPersona.HISTORY_BUFF, db_url)` then `get_persona("test_user_1", db_url)` returns `UserPersona.HISTORY_BUFF`. `get_persona("nonexistent_user", db_url)` returns None without exception. Table created in Postgres without error. | P1 | 41, 19 | 2 hrs | Week 2 |
| 45 | Storied | Development | Claude | Add `POST /user/persona` and `GET /user/persona` endpoints to tour-generator service | Input: `persona_preference_store.py` (#44), existing Flask route patterns in `tour_orchestrator_service.py` or equivalent service file. Work: add two endpoints to the tour-generator Flask app: `POST /user/persona` (body: `{user_id, persona}`) → calls `save_persona()`, returns 200 `{saved: true}`; `GET /user/persona?user_id=X` → calls `get_persona()`, returns 200 `{persona: "art_lover"}` or 404 `{persona: null}`. Both endpoints require the existing API key header. Output: updated service file committed. | `curl -X POST .../user/persona -d '{"user_id":"u1","persona":"art_lover"}'` returns 200. `curl .../user/persona?user_id=u1` returns `{"persona":"art_lover"}`. `curl .../user/persona?user_id=unknown` returns 404. Missing API key returns 401. | P1 | 44 | 2 hrs | Week 3 |
| 46 | Storied | Development | Claude | Wire persona lookup into `generate_tour_text()` service call path | Input: `generate_tour_text.py` after #43, `persona_preference_store.py` (#44), `tour_orchestrator_service.py`. Work: in the service layer that calls `generate_tour_text()`, extract `user_id` from the request (if present), call `get_persona(user_id, db_url)` before invoking `generate_tour_text()`, pass the result as the `persona` parameter. If `user_id` absent or `get_persona()` returns None: `persona=None` (graceful degradation, no error). Output: updated service file committed. | Request with known `user_id` that has saved persona "history_buff": tour generated with history-weighted story types (verified in service log). Request without `user_id`: tour generated with `persona=None`, no error. Request with `user_id` but no saved persona: tour generated with `persona=None`, no error. | P1 | 45, 43 | 1 hr | Week 3 |
| 47 | Storied | Development | Claude | Write `generate_shareable_tour_id(location, tour_type, total_stops)` in new file `tour_sharing.py` | Input: `tour_cache_layer1.py` (#19) SHA256 pattern, `STORIED_DEVELOPMENT_PLAN.md` feature #4. Work: write `development/tour_sharing.py` with `generate_shareable_tour_id(location, tour_type, total_stops)` — generates a short (8-char) alphanumeric ID derived from SHA256 of the cache key, URL-safe (base62 encoding, no special chars). Also write `build_share_url(tour_id, base_url)` → returns `f"{base_url}/tour/{tour_id}"`. Output: `tour_sharing.py` committed. | `generate_shareable_tour_id("Chagall Nice", "museum", 10)` returns 8-char alphanumeric string, no special chars. Same inputs always return same ID (deterministic). Different inputs return different IDs. `build_share_url("abc12345", "https://audioura.io")` returns `"https://audioura.io/tour/abc12345"`. | P1 | 19 | 1 hr | Week 3 |
| 48 | Storied | Development | Claude | Write `store_shared_tour(tour_id, tour_text, metadata, db_url)` and `get_shared_tour(tour_id, db_url)` in `tour_sharing.py` | Input: `tour_sharing.py` (#47), `tour_cache_layer1.py` (#19) Postgres pattern. Work: add `store_shared_tour()` — upserts into `shared_tours` table `(tour_id TEXT PRIMARY KEY, tour_text TEXT, location TEXT, tour_type TEXT, total_stops INT, created_at TIMESTAMP, share_count INT DEFAULT 0)`; and `get_shared_tour()` — returns full row dict or None. Creates table if not exists. Output: updated `tour_sharing.py` committed. | `store_shared_tour("abc12345", tour_text, {location, tour_type, total_stops}, db_url)` then `get_shared_tour("abc12345", db_url)` returns dict with all fields. `get_shared_tour("nonexistent", db_url)` returns None. Table created without error. `share_count` defaults to 0. | P1 | 47 | 2 hrs | Week 3 |
| 49 | Storied | Development | Claude | Add `POST /tour/share` endpoint — generate share ID, store tour, return share URL | Input: `tour_sharing.py` (#48), tour-generator service Flask app. Work: add `POST /tour/share` endpoint — body: `{location, tour_type, total_stops, tour_text}`. Calls `generate_shareable_tour_id()`, `store_shared_tour()`, `build_share_url()` with configured `BASE_URL` env var. Returns 200 `{share_id, share_url}`. If tour already cached (same ID), returns existing URL without re-storing. Requires API key header. Output: updated service file committed. | `POST /tour/share` with valid body returns 200 `{share_id: "abc12345", share_url: "https://audioura.io/tour/abc12345"}`. Second call with same inputs returns same share_id (idempotent). Missing API key returns 401. Missing body fields returns 400. | P1 | 48 | 2 hrs | Week 3 |
| 50 | Storied | Development | Claude | Add `GET /tour/{tour_id}` endpoint — retrieve shared tour by ID | Input: `tour_sharing.py` (#48), tour-generator service Flask app. Work: add `GET /tour/<tour_id>` endpoint — calls `get_shared_tour(tour_id, db_url)`. If found: increments `share_count` by 1, returns 200 with full tour text and metadata `{tour_text, location, tour_type, total_stops, share_count}`. If not found: returns 404 `{error: "tour not found"}`. No API key required (public read — shared tours are intentionally public). Output: updated service file committed. | `GET /tour/abc12345` returns 200 with tour_text. Repeated calls increment share_count. `GET /tour/nonexistent` returns 404. Endpoint accessible without API key header. | P1 | 49 | 1 hr | Week 3 |
| 51 | Storied | Development | Claude | Write `generate_referral_code(user_id)` and `store_referral(referral_code, referrer_user_id, db_url)` in new file `referral_engine.py` | Input: `STORIED_DEVELOPMENT_PLAN.md` feature #4 referral section, `tour_sharing.py` (#47) ID generation pattern. Work: write `development/referral_engine.py` with: `generate_referral_code(user_id)` → deterministic 6-char alphanumeric code from user_id hash; `store_referral(referral_code, referrer_user_id, db_url)` → upsert into `referral_codes(code TEXT PRIMARY KEY, referrer_user_id TEXT, created_at TIMESTAMP, redemption_count INT DEFAULT 0)`; `record_referral_redemption(referral_code, new_user_id, db_url)` → increments redemption_count, inserts into `referral_redemptions(referral_code, new_user_id, redeemed_at TIMESTAMP)`. Output: `referral_engine.py` committed. | `generate_referral_code("user123")` returns 6-char alphanumeric, same input always same output. `store_referral(code, "user123", db_url)` then `record_referral_redemption(code, "new_user", db_url)` succeeds; redemption_count incremented to 1. Tables created without error. | P2 | 47 | 3 hrs | Week 3 |
| 52 | Storied | Development | Claude | Add `POST /referral/create` and `POST /referral/redeem` endpoints to tour-generator service | Input: `referral_engine.py` (#51), tour-generator service Flask app. Work: add `POST /referral/create` (body: `{user_id}`) → calls `generate_referral_code()` + `store_referral()`, returns `{referral_code, referral_url}`; add `POST /referral/redeem` (body: `{referral_code, new_user_id}`) → calls `record_referral_redemption()`, returns 200 `{redeemed: true, referrer_user_id}` or 404 if code unknown. Both require API key. Output: updated service file committed. | `POST /referral/create` with `{user_id: "u1"}` returns `{referral_code: "abc123", referral_url: "https://audioura.io/join/abc123"}`. `POST /referral/redeem` with valid code + new_user_id returns 200. `POST /referral/redeem` with unknown code returns 404. Missing API key returns 401. | P2 | 51 | 2 hrs | Week 3 |
| 53 | Storied | Development | Claude | Write `attestation_verifier.py` — Play Integrity + App Attest token log-only validator | Input: `STORIED_DEVELOPMENT_PLAN.md` feature #5 (log-only first), `claude_review_app_attestation_and_budget_for_kiro_2026_06_11.md`, `claude_review_attestation_scaffold_contract_kiro_2026_06_11.md`. Work: write `development/attestation_verifier.py` with `verify_attestation_token(token, platform, request_id)` — parses JWT structure of token (no actual Google/Apple verification call in this task — that is task #54), logs result to stdout as `ATTESTATION LOG: platform={platform} request_id={request_id} token_present={bool} token_length={int} verdict=log_only`. Returns `{verified: True, log_only: True}` always (never blocks). Output: `attestation_verifier.py` committed. | `verify_attestation_token("fake.jwt.token", "android", "req_001")` returns `{verified: True, log_only: True}` and prints `ATTESTATION LOG:` line. `verify_attestation_token(None, "ios", "req_002")` returns `{verified: True, log_only: True}` and logs `token_present=False`. No exception on any input. | P1 | — | 2 hrs | Week 3 |
| 54 | Storied | Development | Claude | Extend `attestation_verifier.py` with Play Integrity API call (log-only, Android) | Input: `attestation_verifier.py` (#53), Google Play Integrity API docs (public). Work: add `verify_play_integrity(token, package_name, api_key)` — calls Google Play Integrity API `POST https://playintegrity.googleapis.com/v1/{package_name}:decodeIntegrityToken` with the token. Parses `deviceIntegrity.deviceRecognitionVerdict` and `appIntegrity.appRecognitionVerdict`. Logs full verdict to stdout as `PLAY_INTEGRITY_VERDICT: {verdict_json}`. Returns `{verified: True, log_only: True, verdict: dict}` — NEVER returns False or blocks the request. Handles API errors by logging and returning `{verified: True, log_only: True, verdict: None}`. Output: updated `attestation_verifier.py` committed. | `verify_play_integrity` with invalid token: logs error, returns `{verified: True, log_only: True, verdict: None}` without raising exception. Stdout contains `PLAY_INTEGRITY_VERDICT:` line. Function signature accepts all 3 parameters. No request is blocked regardless of verdict. | P1 | 53 | 3 hrs | Week 4 |
| 55 | Storied | Development | Claude | Extend `attestation_verifier.py` with App Attest verification (log-only, iOS) | Input: `attestation_verifier.py` (#54), Apple App Attest API docs (public). Work: add `verify_app_attest(attestation_object, key_id, app_id)` — validates the CBOR-encoded attestation object structure (format check only — no Apple server call required for log-only): checks that `fmt` field is `"apple-appattest"`, that `authData` is present, logs `APP_ATTEST_VERDICT: key_id={key_id} fmt_valid={bool} auth_data_present={bool}`. Returns `{verified: True, log_only: True}` always. Handles malformed input gracefully. Output: updated `attestation_verifier.py` committed. | `verify_app_attest(valid_cbor_bytes, "key_001", "com.audioura.app")` logs `APP_ATTEST_VERDICT:` line and returns `{verified: True, log_only: True}`. `verify_app_attest(b"garbage", "key_001", "com.audioura.app")` logs error and returns `{verified: True, log_only: True}` without exception. | P1 | 54 | 3 hrs | Week 4 |
| 56 | Storied | Development | Claude | Wire attestation logging into API gateway under `ATTESTATION_MODE=log_only` env var | Input: `attestation_verifier.py` (#55), gateway service (API key enforcement layer — identified from `GATEWAY_API_KEY_PURPOSE.md`). Work: add attestation header extraction to gateway request handler — read `X-Attestation-Token` and `X-Attestation-Platform` headers (Android sends Play Integrity token; iOS sends App Attest token). When `ATTESTATION_MODE=log_only`: call `verify_attestation_token()` for structural log, call platform-specific verifier, log result, ALWAYS pass request through (never 401/403 for attestation). When `ATTESTATION_MODE` absent or `off`: skip entirely. Output: updated gateway service file committed. | Request with valid Android token + `ATTESTATION_MODE=log_only`: gateway logs `ATTESTATION LOG:` and `PLAY_INTEGRITY_VERDICT:`, returns normal response. Request with no attestation header + `ATTESTATION_MODE=log_only`: logs `token_present=False`, returns normal response. Request without `ATTESTATION_MODE` set: no attestation logs, normal response. No request is ever rejected due to attestation in log_only mode. | P1 | 55 | 3 hrs | Week 4 |
| 57 | Storied | Development | Claude | Write `test_attestation_log_only.py` — verify attestation never blocks requests | Input: `attestation_verifier.py` (#55), gateway service after #56. Work: write `development/test_attestation_log_only.py` — sends 4 test requests to the gateway (running locally): (1) valid API key + valid mock attestation token, (2) valid API key + no attestation token, (3) valid API key + malformed attestation token, (4) valid API key + wrong platform header. Asserts all 4 return HTTP 200 (or whatever the normal response is — not 401/403). Asserts stdout contains `ATTESTATION LOG:` for all 4. Output: test script committed. | `python test_attestation_log_only.py` with local gateway running exits 0. All 4 assertions pass. No request returns 401 or 403 due to attestation header content. | P1 | 56 | 1 hr | Week 4 |
| 58 | Storied | Development | Claude | Write `attestation_enforce_gate.py` — enforce mode stub (NOT activated for Aug 1) | Input: `attestation_verifier.py` (#55), `STORIED_DEVELOPMENT_PLAN.md` feature #5 "flip to enforce as an update during the test window". Work: write `development/attestation_enforce_gate.py` with `should_block_request(verdict_dict, platform)` — returns True if verdict indicates UNEVALUATED_INTEGRITY or FAILED_MEETS_DEVICE_INTEGRITY AND `ATTESTATION_MODE=enforce`. Returns False in all other cases including when `ATTESTATION_MODE != enforce`. Include clear docstring: "DO NOT wire into gateway until after Aug 1 tester build is live and log-only data has been reviewed." Output: `attestation_enforce_gate.py` committed, NOT imported by gateway. | `should_block_request({"verdict": "UNEVALUATED"}, "android")` with `ATTESTATION_MODE=log_only` returns False. `should_block_request({"verdict": "UNEVALUATED"}, "android")` with `ATTESTATION_MODE=enforce` returns True. Gateway service file does NOT import `attestation_enforce_gate`. | P2 | 57 | 1 hr | Week 4 |
| 59 | Storied | Development | Claude | Add `STORIED_MODE`, `ATTESTATION_MODE`, `BASE_URL`, `REFERRAL_BASE_URL` env vars to all relevant service Dockerfiles and docker-compose | Input: all service Dockerfiles in `development/`, `docker-compose.yml`, tasks #12 (STORIED_MODE), #56 (ATTESTATION_MODE), #49 (BASE_URL), #52 (REFERRAL_BASE_URL). Work: audit all service Dockerfiles and docker-compose for missing env vars. Add: `STORIED_MODE=false`, `ATTESTATION_MODE=log_only`, `BASE_URL=http://localhost:5000`, `REFERRAL_BASE_URL=http://localhost:5000` to relevant services (tour-generator + gateway). Output: updated Dockerfiles and docker-compose committed. | `docker exec development-tour-generator-1 printenv STORIED_MODE` returns `false`. `docker exec development-gateway-1 printenv ATTESTATION_MODE` returns `log_only`. All containers rebuild without error. No existing env vars overwritten. | P1 | 56, 12 | 1 hr | Week 4 |
| 60 | Storied | Development | Claude | Write `storied_smoke_test.py` — end-to-end smoke test covering all 5 Storied features | Input: all service endpoints from tasks #45, #49, #50, #52, `generate_tour_text.py` with `STORIED_MODE=true`, `test_attestation_log_only.py` (#57). Work: write `development/storied_smoke_test.py` — runs against locally running containers, tests: (1) tour generated with `STORIED_MODE=true` completes without error, (2) `POST /user/persona` saves and `GET /user/persona` retrieves, (3) `POST /tour/share` returns share_url, (4) `GET /tour/{id}` retrieves shared tour, (5) `POST /referral/create` returns referral_code, (6) gateway with attestation header returns 200 (log_only). Prints PASS/FAIL per test. Output: `storied_smoke_test.py` committed. | `python storied_smoke_test.py` with all containers running exits 0, all 6 tests PASS. Any single service being down causes that test to FAIL (not crash entire script). | P1 | 59, 57, 50, 52, 46 | 3 hrs | Week 4 |
