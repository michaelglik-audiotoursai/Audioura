# Audioura Release: Storied — Sprint 1 Batch 1 (Tasks 1–20)

| # | Release | Track | Owner | Task name | Description (inputs → work → outputs) | Acceptance criteria | Priority | Depends on (#) | Est. effort | Target week |
|---|---------|-------|-------|-----------|----------------------------------------|---------------------|----------|----------------|-------------|-------------|
| 1 | Storied | Development | Claude | Create spine template file for museum tours | Input: `chagall_spine_poc.json` + `remind_advisor.md` architecture section. Work: extract the JSON schema from the POC into a reusable prompt template file `development/templates/spine_museum.txt` with named variable slots (`{{venue_name}}`, `{{poi_list}}`, `{{total_stops}}`). Output: `templates/spine_museum.txt` committed to `services-migration`. | File exists, contains all 11 spine fields (tour_hook, connecting_thread, arc[].chapter_role, emotional_beat, unique_angle, plant, callback, cliffhanger, climax_stop, resolution_stop, closing_revelation), renders correctly when variables substituted manually. | P1 | — | 1 hr | Week 1 |
| 2 | Storied | Development | Claude | Create spine template files for walking, restaurant, book tour types | Input: `spine_museum.txt` (#1) as pattern. Work: produce `spine_walking.txt`, `spine_restaurant.txt`, `spine_book.txt` with tour-type-appropriate chapter_role vocabulary and connecting_thread guidance per type (see architecture table in `remind_advisor.md`). Output: 3 template files committed. | All 3 files exist, each has distinct chapter_role set appropriate to tour type, variable slots identical to museum template for consistency. | P2 | 1 | 1 hr | Week 1 |
| 3 | Storied | Development | Claude | Write `generate_spine()` function in new file `spine_generator.py` | Input: `spine_museum.txt` (#1), `spine_poc.py`, OpenAI API pattern from `generate_tour_text.py`. Work: write `development/spine_generator.py` with `generate_spine(venue_name, poi_list, tour_category, api_key)` — loads correct template by `tour_category`, substitutes variables, calls gpt-4o, returns parsed JSON spine or None on failure. Measures and logs cost+latency. Output: `spine_generator.py` committed. | Function callable from Python shell inside container; given Chagall 10-POI list returns valid JSON with all 11 fields in under 20s; cost logged to stdout. | P1 | 1 | 3 hrs | Week 1 |
| 4 | Storied | Development | Claude | Write unit test script `test_spine_generator.py` for `generate_spine()` | Input: `spine_generator.py` (#3), `chagall_spine_poc.json` as expected-shape reference. Work: write `development/test_spine_generator.py` — runs `generate_spine()` with Chagall inputs, asserts all required JSON keys present, asserts `climax_stop` is integer between 1 and total_stops, asserts no stop has null `emotional_beat`, prints PASS/FAIL. Output: test script committed. | `docker exec development-tour-generator-1 python test_spine_generator.py` exits 0 and prints PASS for all assertions. | P1 | 3 | 1 hr | Week 1 |
| 5 | Storied | Development | Claude | Write `fetch_wikipedia_summary(topic, sentences)` utility in `rag_retriever.py` | Input: Wikipedia REST API docs (public). Work: write `development/rag_retriever.py` with `fetch_wikipedia_summary(topic, sentences=5)` — calls `https://en.wikipedia.org/api/rest_v1/page/summary/{topic}`, returns `extract` field (plain text), handles 404/redirect gracefully by returning empty string. No OpenAI call. Output: `rag_retriever.py` committed. | Given `fetch_wikipedia_summary("Marc Chagall", 5)` returns non-empty string containing "Chagall" in under 3s; given non-existent topic returns empty string without raising exception. | P1 | — | 2 hrs | Week 1 |
| 6 | Storied | Development | Claude | Write `fetch_poi_rag_context(poi_name, venue_name, tour_category)` in `rag_retriever.py` | Input: `rag_retriever.py` (#5). Work: add `fetch_poi_rag_context(poi_name, venue_name, tour_category)` — builds 2 Wikipedia lookup topics (artist/creator topic + period/context topic), calls `fetch_wikipedia_summary()` for each, returns dict `{artist_context: str, period_context: str}`. For museum tours uses venue_name as secondary lookup. Output: updated `rag_retriever.py` committed. | Given `fetch_poi_rag_context("Biblical Message Room", "Musée National Marc Chagall", "museum")` returns dict with both keys non-empty and combined length > 200 chars. | P1 | 5 | 2 hrs | Week 1 |
| 7 | Storied | Development | Claude | Write `generate_fact_sheet(poi_name, rag_context, api_key)` in new file `fact_extractor.py` | Input: `rag_retriever.py` (#6), existing `generate_tour_text.py` API call pattern. Work: write `development/fact_extractor.py` with `generate_fact_sheet(poi_name, rag_context, api_key)` — builds prompt with RAG context, calls gpt-3.5-turbo at temperature=0.1, returns structured JSON `{confirmed_facts: [], uncertain_facts: [], date_created: str, medium: str, surprising_detail: str}`. Output: `fact_extractor.py` committed. | Given Chagall "Biblical Message Room" + Wikipedia RAG context, returns valid JSON with at least 2 confirmed_facts, non-empty surprising_detail, cost < $0.003. | P1 | 6 | 3 hrs | Week 2 |
| 8 | Storied | Development | Claude | Write `generate_fact_sheets_parallel(poi_list, venue_name, tour_category, api_key)` in `fact_extractor.py` | Input: `fact_extractor.py` (#7), `rag_retriever.py` (#6), ThreadPoolExecutor pattern from `generate_tour_text.py`. Work: add parallel wrapper — fetches RAG context and generates fact sheet for each POI using ThreadPoolExecutor(max_workers=5), returns list of fact sheets in original POI order. Output: updated `fact_extractor.py` committed. | Given 10-stop Chagall poi_list, all 10 fact sheets returned in correct order, total wall-clock time < 30s, no exception on any individual POI failure (graceful None per failed stop). | P1 | 7 | 2 hrs | Week 2 |
| 9 | Storied | Development | Claude | Modify `_generate_description()` in `generate_tour_text.py` to accept spine context injection | Input: `generate_tour_text.py` (current Phase 5), `spine_generator.py` (#3). Work: modify `_generate_description()` signature to accept optional `spine_stop` dict (single arc entry from spine JSON). If `spine_stop` provided, append spine context block to `description_prompt` before the format instructions — injecting `emotional_beat`, `unique_angle`, `cliffhanger`, and any `callback` text. If `spine_stop` is None, behavior is unchanged (Beta pipeline preserved). Output: modified `generate_tour_text.py` committed. | (a) When called with `spine_stop=None`, output is byte-for-byte identical to current Beta behavior. (b) When called with Chagall Stop 1 spine entry, generated description contains language echoing the `unique_angle` and ends with a forward-looking question matching the `cliffhanger` field. | P1 | 3, 4 | 2 hrs | Week 2 |
| 10 | Storied | Development | Claude | Modify `_generate_description()` in `generate_tour_text.py` to accept fact sheet injection | Input: `generate_tour_text.py` after #9, `fact_extractor.py` (#8). Work: extend `_generate_description()` to accept optional `fact_sheet` dict. If provided, prepend confirmed_facts as a "verified facts" block and append `surprising_detail` as a mandatory inclusion instruction. If `fact_sheet` is None, behavior unchanged. Output: updated `generate_tour_text.py` committed. | (a) When called with `fact_sheet=None`, output identical to post-#9 behavior. (b) When called with Chagall Stop 1 fact sheet containing a real surprising_detail, that detail (or close paraphrase) appears in the generated description text. | P1 | 9 | 2 hrs | Week 2 |
| 11 | Storied | Development | Claude | Wire spine + fact sheets into `generate_tour_text()` main pipeline under `STORIED_MODE` feature flag | Input: `generate_tour_text.py` after #10, `spine_generator.py` (#3), `fact_extractor.py` (#8). Work: read env var `STORIED_MODE` (default `"false"`). If `"true"`: after Phase 3B, call `generate_spine()` and `generate_fact_sheets_parallel()`, then pass matching spine_stop + fact_sheet into each `_generate_description()` call. If `"false"` or env var missing: pipeline runs exactly as today. Output: updated `generate_tour_text.py` committed. | With `STORIED_MODE=false`, full Chagall tour output is identical to `chagall_current_tour.txt`. With `STORIED_MODE=true`, tour completes without error and each stop description is observably differentiated (different opening sentences, emotional vocabulary varies per stop). | P1 | 10 | 3 hrs | Week 2 |
| 12 | Storied | Development | Claude | Add `STORIED_MODE` env var to `development-tour-generator-1` container Dockerfile and docker-compose | Input: `generate_tour_text.py` after #11, existing `docker-compose.yml` and Dockerfile for tour-generator service. Work: add `STORIED_MODE=false` to Dockerfile ENV and docker-compose environment block for `development-tour-generator-1`. Output: updated `Dockerfile` and `docker-compose.yml` committed. Do NOT set to `true` yet — flag stays off until validation complete. | `docker exec development-tour-generator-1 printenv STORIED_MODE` returns `false`. Container rebuilds without error. Tour generation behaves identically to current (flag off). | P1 | 11 | 30 min | Week 2 |
| 13 | Storied | Development | Claude | Write end-to-end validation script `validate_storied_tour.py` | Input: all Storied components (#3–#12), `chagall_current_tour.txt` as baseline. Work: write `development/validate_storied_tour.py` — generates a Storied tour for Chagall with `STORIED_MODE=true`, then runs 5 automated checks: (1) all 10 stops present, (2) no two stops share the same opening sentence, (3) each stop description contains at least one number or proper noun not present in the baseline (fact injection signal), (4) total cost < $0.10, (5) total time < 120s. Prints PASS/FAIL per check. Output: script committed. | Script runs inside container, all 5 checks PASS, exits 0. | P1 | 12 | 2 hrs | Week 3 |
| 14 | Storied | Development | Claude | Write `spine_quality_scorer.py` — automated spine quality check | Input: `chagall_spine_poc.json` as reference, `spine_generator.py` (#3). Work: write `development/spine_quality_scorer.py` — given a generated spine JSON, scores: (1) climax_stop is in range [total_stops*0.5, total_stops*0.8], (2) no two stops share identical emotional_beat, (3) callback fields reference actual prior stop names, (4) closing_revelation length > 50 chars. Returns score 0–4 and per-check breakdown. Output: script committed. | Given `chagall_spine_poc.json`, returns score 3 or 4 (known: closing_revelation is weak = 1 expected fail). Given a spine with all identical emotional_beats, returns score ≤ 2. | P2 | 3 | 2 hrs | Week 3 |
| 15 | Storied | Development | Claude | Strengthen closing_revelation prompt in `spine_museum.txt` template | Input: `spine_museum.txt` (#1), `chagall_spine_poc.json` weakness identified in Session 6 (closing_revelation too abstract). Work: revise the closing_revelation instruction in the template to explicitly require: a specific verifiable fact about the venue/artist, avoidance of generic words ("eternal", "dialogue", "testament"), and a concrete image or action the visitor can carry away. Output: updated `spine_museum.txt` committed, test via `spine_quality_scorer.py`. | After template update, `generate_spine()` for Chagall returns closing_revelation with at least one specific proper noun or verifiable fact; `spine_quality_scorer.py` scores closing_revelation check as PASS. | P2 | 14 | 1 hr | Week 3 |
| 16 | Storied | Development | Claude | Write `generate_perspective_layer(poi_name, layer_type, rag_context, spine_stop, api_key)` in new file `perspective_generator.py` | Input: `rag_retriever.py` (#6), `remind_advisor.md` Layer 3 architecture. Work: write `development/perspective_generator.py` with function accepting `layer_type` ∈ {artist, historian, curator} — selects layer-appropriate system prompt, injects RAG context + spine emotional_beat as framing, calls gpt-3.5-turbo, returns 80–120 word perspective text. Output: `perspective_generator.py` committed. | Given Chagall "Biblical Message Room", layer_type="artist", valid RAG context: returns non-empty string 80–120 words, contains at least one detail from the RAG context (not pure hallucination), cost < $0.003. | P2 | 6, 3 | 3 hrs | Week 3 |
| 17 | Storied | Development | Claude | Write `generate_all_perspectives_parallel(poi_list, rag_contexts, spine, api_key)` in `perspective_generator.py` | Input: `perspective_generator.py` (#16). Work: add parallel wrapper generating all 3 layers × N stops using ThreadPoolExecutor(max_workers=5), returns list of dicts `{stop_index, artist_layer, historian_layer, curator_layer}` in original order. Individual layer failures return empty string (no exception propagation). Output: updated `perspective_generator.py` committed. | Given 10-stop Chagall list: all 30 layers generated (3×10), total time < 45s, any single API failure produces empty string for that layer only, no exception raised, cost < $0.04 total. | P2 | 16 | 2 hrs | Week 3 |
| 18 | Storied | Development | Claude | Add perspective layers to Phase 6 assembly in `generate_tour_text.py` under `STORIED_MODE` flag | Input: `generate_tour_text.py` after #12, `perspective_generator.py` (#17). Work: when `STORIED_MODE=true`, after Phase 5 descriptions, call `generate_all_perspectives_parallel()` and append perspective layers to each stop's assembled text block, formatted as three clearly labeled sections: `🎨 Artist's View:`, `📚 Historian's View:`, `👁️ Curator's View:`. When `STORIED_MODE=false`, assembly unchanged. Output: updated `generate_tour_text.py` committed. | With `STORIED_MODE=true`, Chagall tour output contains all three perspective labels on each of the 10 stops; with `STORIED_MODE=false`, none appear. | P2 | 17, 11 | 2 hrs | Week 4 |
| 19 | Storied | Development | Claude | Write `tour_cache_layer1.py` — exact-match tour cache using Postgres | Input: existing Postgres schema in `development-postgres-2-1` container, `generate_tour_text.py` API call pattern. Work: write `development/tour_cache_layer1.py` with `get_cached_tour(location, tour_type, total_stops, db_url)` (returns cached tour text or None) and `store_tour(location, tour_type, total_stops, tour_text, db_url)` (upserts). Cache key = SHA256 of `f"{location}|{tour_type}|{total_stops}"`. Creates table `tour_cache` if not exists. Output: `tour_cache_layer1.py` committed. | Store then immediately get same inputs returns original text. Different total_stops returns None (cache miss). Table created in Postgres without error. | P2 | — | 3 hrs | Week 4 |
| 20 | Storied | Development | Claude | Wire `tour_cache_layer1.py` into `generate_tour_text()` — check before Phase 1, store after Phase 6 | Input: `generate_tour_text.py` after #18, `tour_cache_layer1.py` (#19). Work: at function entry (before Phase 1 intent analysis), call `get_cached_tour()` — if hit, return cached text immediately with log line `CACHE HIT`. After Phase 6 assembly, call `store_tour()`. Cache only active when `STORIED_MODE=true` (Beta pipeline untouched). DB_URL read from `DATABASE_URL` env var; if env var absent, cache silently skipped. Output: updated `generate_tour_text.py` committed. | First call generates tour normally (CACHE MISS logged). Second identical call returns in < 1s (CACHE HIT logged). If `DATABASE_URL` unset, tour generates normally with no error. `STORIED_MODE=false` never touches cache. | P2 | 19, 18 | 2 hrs | Week 4 |
