flask==2.3.3
flask-cors==4.0.0
openai==0.27.0
# 2.9.1 ships no cp311 wheel, so Dockerfile.tour-worker (python:3.11-slim) fell back to a
# source build and failed on missing pg_config. 2.9.9 has cp39 and cp311 wheels and is
# already the pin in requirements_generator / requirements-orchestrator / -tour-id-resolution.
psycopg2-binary==2.9.9
requests==2.28.1
boto3>=1.26.0
google-cloud-tasks==2.16.0
google-cloud-kms==2.21.0
cryptography>=41.0.0