# Audioura Release: Storied — Sprint 1 Batch 4 (Tasks 61–80)

| # | Release | Track | Owner | Task name | Description (inputs → work → outputs) | Acceptance criteria | Priority | Depends on (#) | Est. effort | Target week |
|---|---------|-------|-------|-----------|----------------------------------------|---------------------|----------|----------------|-------------|-------------|
| 61 | Storied | Development | Claude | Write `privacy_disclosure_delta.md` — document all new data collected by Storied features | Input: `STORIED_DEVELOPMENT_PLAN.md` feature #3 privacy note, `onboarding_preference.py` (#41), `persona_preference_store.py` (#44), `referral_engine.py` (#51), `attestation_verifier.py` (#53). Work: write `development/privacy_disclosure_delta.md` listing every new data point collected by Storied vs Beta: (a) user persona preference (onboarding choice), (b) referral code + redemption linkage, (c) attestation token (not stored — logged only), (d) share_count per tour. For each: what is collected, why, how long retained, which third party receives it (if any). Format as a table. Output: `privacy_disclosure_delta.md` committed. | File exists. Table contains all 4 data points. Each row has: data_point, purpose, retention_period, third_party columns. No OpenAI or Google field is missing. File is suitable for Sir Michael to use when updating privacy policy copy. | P1 | 44, 51, 53 | 2 hrs | Week 3 |
| 62 | Storied | Development | Claude | Write `data_safety_storied_delta.md` — Android Data Safety form additions for Storied | Input: `privacy_disclosure_delta.md` (#61), `AUDIOURA_DATA_SAFETY_MAPPING.md`. Work: write `development/data_safety_storied_delta.md` — for each new Storied data point, provide the exact Google Play Data Safety form answer: data type category (e.g. "App activity"), data subtype, collected (yes/no), shared with third parties (yes/no), optional or required, purpose. Format matches the existing `AUDIOURA_DATA_SAFETY_MAPPING.md` structure so it can be merged directly. Output: `data_safety_storied_delta.md` committed. | File exists. All 4 data points from `privacy_disclosure_delta.md` have corresponding Data Safety rows. Each row specifies all required Google Play fields. No new field uses "third party = yes" without naming the third party. | P1 | 61 | 1 hr | Week 3 |
| 63 | Storied | Development | Claude | Write `app_privacy_storied_delta.md` — Apple App Privacy nutrition label additions for Storied | Input: `privacy_disclosure_delta.md` (#61), existing Apple privacy label format from `STORIED_DEVELOPMENT_PLAN.md`. Work: write `development/app_privacy_storied_delta.md` — for each new Storied data point, provide the exact App Store Connect privacy label answer: data type, linked to identity (yes/no), used for tracking (yes/no), purpose. Explicitly note that persona preference IS behavioral profiling per Apple's definition and must be declared. Output: `app_privacy_storied_delta.md` committed. | File exists. All 4 data points covered. Persona preference row marks "linked to identity = yes" and "purpose = App Functionality + Analytics". Attestation token row marks "collected = no" (not stored). File includes a one-paragraph plain-English summary Sir Michael can paste into App Store Connect review notes. | P1 | 61 | 1 hr | Week 3 |
| 64 | Storied | Development | Claude | Write `regression_beta_parity.py` — verify Beta pipeline is unchanged when `STORIED_MODE=false` | Input: `generate_tour_text.py` (Storied branch after all pipeline modifications), `chagall_current_tour.txt` (Beta baseline output). Work: write `development/regression_beta_parity.py` — generates a fresh tour for the Chagall inputs with `STORIED_MODE=false`, compares output structure (not exact text — GPT is non-deterministic) against `chagall_current_tour.txt`: asserts (1) same number of stops, (2) same stop names in same order, (3) no "Introduction:" block present, (4) no `🎨 Artist's View:` labels present, (5) no `STORIED` or `SPINE` in output text, (6) total cost within 20% of baseline cost. Prints PASS/FAIL per assertion. Output: script committed. | `python regression_beta_parity.py` inside container exits 0, all 6 assertions PASS. Confirms Storied modifications have zero impact on `STORIED_MODE=false` behavior. | P1 | 13, 11 | 2 hrs | Week 3 |
| 65 | Storied | Development | Claude | Write `regression_all_tour_types.py` — Beta parity regression across all 4 tour categories | Input: `regression_beta_parity.py` (#64), `_classify_tour_category()` from `generate_tour_text.py`. Work: extend regression to run with `STORIED_MODE=false` for all 4 tour categories: museum (Chagall Nice), walking (Beacon Hill Boston 8 stops), restaurant (North End Boston 8 stops), book/specialized (Harry Potter London 8 stops). For each: assert stop count matches expected, no Storied artifacts in output, cost within 20% of known Beta baseline. Output: `regression_all_tour_types.py` committed, results printed per tour type. | All 4 tour types pass all 6 assertions each (24 total assertions). Script exits 0. Any single failure prints which tour type and which assertion failed before exiting 1. | P1 | 64 | 2 hrs | Week 4 |
| 66 | Storied | Development | Claude | Write `integration_test_storied_full.py` — full pipeline integration test with all features active | Input: `storied_smoke_test.py` (#60), `content_qa_runner.py` (#39), `regression_beta_parity.py` (#64). Work: write `development/integration_test_storied_full.py` — orchestrates a full end-to-end test with `STORIED_MODE=true`: (1) generate Chagall museum tour with persona="art_lover", (2) run `content_qa_runner.py` checks on output, (3) share the tour via `POST /tour/share`, (4) retrieve via `GET /tour/{id}`, (5) save + retrieve persona, (6) create referral code, (7) verify attestation logging fires, (8) run `regression_beta_parity.py` with `STORIED_MODE=false` immediately after. All 8 steps must pass. Output: `integration_test_storied_full.py` committed. | `python integration_test_storied_full.py` exits 0. All 8 steps PASS. Regression step (8) confirms Beta behavior unchanged in same run. Total runtime < 5 minutes. | P1 | 60, 39, 64 | 3 hrs | Week 4 |
| 67 | Storied | Development | Claude | Write `cost_ceiling_monitor.py` — per-tour cost guard for Storied pipeline | Input: `generate_tour_text.py` (Storied branch), `NEW_ARCHITECTURE_STORIED_SPLIT.md` cost target (~+$0.07/tour). Work: write `development/cost_ceiling_monitor.py` with `check_cost_ceiling(total_cost, tour_category, storied_mode)` — if `storied_mode=true` and `total_cost > 0.15` (hardcoded ceiling), logs `COST CEILING EXCEEDED: ${total_cost:.4f}` and returns `{exceeded: True}`. If within ceiling, logs `COST OK: ${total_cost:.4f}` and returns `{exceeded: False}`. Wire into `generate_tour_text()` after Phase 6 assembly — log only, never abort the tour. Output: `cost_ceiling_monitor.py` committed and wired in. | `check_cost_ceiling(0.20, "museum", True)` logs `COST CEILING EXCEEDED` and returns `{exceeded: True}`. `check_cost_ceiling(0.08, "museum", True)` logs `COST OK`. `check_cost_ceiling(0.20, "museum", False)` returns `{exceeded: False}` (ceiling only applies to Storied mode). Tour is never aborted regardless of result. | P1 | 11 | 1 hr | Week 3 |
| 68 | Storied | Development | Claude | Write `storied_version_constants.py` — Storied release version management | Input: `STORIED_DEVELOPMENT_PLAN.md` ("Version line: 2.2.0+… kept distinct from Beta's 2.1.x"), `pubspec.yaml` version pattern. Work: write `development/storied_version_constants.py` defining: `STORIED_VERSION = "2.2.0"`, `STORIED_BUILD = 1`, `STORIED_SERVICE_VERSION = "2.2.0.1"`, `STORIED_BRANCH = "storied"`, and `get_version_string()` returning `"2.2.0+1"`. Also write a one-line `validate_version_consistency()` that asserts service version major.minor matches app version major.minor. Output: `storied_version_constants.py` committed. | `get_version_string()` returns `"2.2.0+1"`. `validate_version_consistency()` passes without exception. `STORIED_VERSION` and `STORIED_SERVICE_VERSION` share the same `2.2.0` prefix. Module importable without external dependencies. | P1 | — | 30 min | Week 3 |
| 69 | Storied | Development | Claude | Add `SERVICE_VERSION` constant to all 3 modified service files using Storied version | Input: `storied_version_constants.py` (#68), tour-generator service, gateway service, any other service modified in tasks #45–#56. Work: add `from storied_version_constants import STORIED_SERVICE_VERSION` to each modified service file, set `SERVICE_VERSION = STORIED_SERVICE_VERSION` at module level, include version in health endpoint response: `GET /health` → `{status: "ok", version: "2.2.0.1", mode: os.getenv("STORIED_MODE", "false")}`. Output: all modified service files updated and committed. | `curl http://localhost:5000/health` returns JSON with `version: "2.2.0.1"` and `mode: "false"`. `curl http://localhost:5001/health` (gateway) returns same version. All 3 service files import from `storied_version_constants`. | P1 | 68, 59 | 1 hr | Week 3 |
| 70 | Storied | Development | Claude | Write `storied_release_notes.md` — Storied v2.2.0 release notes for testers | Input: `STORIED_DEVELOPMENT_PLAN.md` 5 features, `NEW_ARCHITECTURE_STORIED_SPLIT.md`, all task outputs #1–#69. Work: write `development/storied_release_notes.md` — tester-facing release notes for v2.2.0: (1) What's new (5 features in plain English, 2–3 sentences each), (2) Known limitations (perspectives deferred to New Architecture, enforce mode not yet active), (3) How to test each feature (step-by-step for tester), (4) Cost expectations ($0.07–$0.15/tour), (5) Privacy changes (links to `privacy_disclosure_delta.md`). Output: `storied_release_notes.md` committed. | File exists. All 5 features described. "Known limitations" section present. Each feature has at least one testable step. Privacy changes section references `privacy_disclosure_delta.md` by filename. Total length 400–800 words (readable, not a wall of text). | P1 | 61 | 2 hrs | Week 4 |
| 71 | Storied | Development | Claude | Write `storied_launch_checklist.md` — Aug 1 pre-submission gate checklist | Input: `LAUNCH_CHECKLIST.md`, `STORIED_DEVELOPMENT_PLAN.md` timeline, `privacy_disclosure_delta.md` (#61), `data_safety_storied_delta.md` (#62), `app_privacy_storied_delta.md` (#63). Work: write `development/storied_launch_checklist.md` — ordered checklist of every gate that must be PASS before submitting Storied to Google Play closed test + Apple TestFlight: integration test (#66) PASS, regression (#65) PASS, content QA all 4 tour types ≥ 7/8 (#40), privacy policy updated (Sir Michael), Data Safety form updated (Sir Michael), App Privacy labels updated (Sir Michael), demo account created, background audio justification written, Android keystore backed up (3 places), `STORIED_MODE=true` in production env, `ATTESTATION_MODE=log_only` confirmed. Output: `storied_launch_checklist.md` committed. | File exists. Contains at least 12 checklist items. Each item has: checkbox `[ ]`, owner (Claude/Michael/Mobile), blocking (yes/no). All automated gate items reference the specific script and exit code that proves PASS. | P1 | 66, 65, 40, 63 | 2 hrs | Week 4 |
| 72 | Storied | Development | Claude | Write `storied_rollback_plan.md` — rollback procedure if Storied breaks in testing | Input: `git_branch_strategy.md`, `STORIED_DEVELOPMENT_PLAN.md` branch model, `STORIED_MODE` feature flag (#12). Work: write `development/storied_rollback_plan.md` with 3-tier rollback: (1) Flag rollback — set `STORIED_MODE=false` in container env and restart, restores Beta behavior in < 2 min, no redeploy needed; (2) Service rollback — `docker-compose stop` then `git checkout services-migration` specific files then `docker-compose start`, restores Beta services in ~5 min; (3) Full branch rollback — revert to `beta-2.1.1+18` tag on both stores. Include exact commands for each tier. Output: `storied_rollback_plan.md` committed. | File exists. All 3 tiers present. Each tier has exact `docker` and `git` commands (no vague instructions). Tier 1 procedure executable in < 2 minutes by Sir Michael without developer assistance. | P1 | 12 | 1 hr | Week 4 |
| 73 | Storied | Development | Claude | Write `storied_cost_report_template.py` — weekly cost monitoring script for Storied period | Input: `cost_ceiling_monitor.py` (#67), `AUDIOURA_CLOUD_MIGRATION_AND_LIFECYCLE.md` cost monitoring section. Work: write `development/storied_cost_report_template.py` — reads service stdout logs from `docker logs development-tour-generator-1` (last 7 days), counts: total tours generated, `STORIED_MODE=true` tours, total cost logged, ceiling exceeded events, cache hits. Prints weekly summary table. Output: `storied_cost_report_template.py` committed. | Script runs without error against live container logs. Outputs a table with all 5 metrics. If no logs found: prints "No tour logs found for period" without exception. Runs in < 10s. | P2 | 67, 20 | 2 hrs | Week 4 |
| 74 | Storied | Development | Claude | Write `test_persona_weighted_tour.py` — verify personalization produces observably different tours | Input: `generate_tour_text.py` (#43), `onboarding_preference.py` (#41). Work: write `development/test_persona_weighted_tour.py` — generates 2 tours for identical Chagall inputs with `STORIED_MODE=true`, one with `persona="art_lover"` and one with `persona="history_buff"`. Runs `story_type_assigner` output comparison: asserts the two tours have different story_type distributions (art_lover has more `art` type, history_buff has more `history` type). Also asserts the two description texts differ by at least 30% (Jaccard distance on word sets). Prints PASS/FAIL. Output: test script committed. | `python test_persona_weighted_tour.py` exits 0. art_lover tour has `art` story_type count ≥ 4/10. history_buff tour has `history` story_type count ≥ 4/10. Text difference ≥ 30%. Both tours complete without error. | P1 | 43, 46 | 2 hrs | Week 4 |
| 75 | Storied | Development | Claude | Write `test_sharing_deep_link.py` — verify share URL resolves to correct tour | Input: `tour_sharing.py` (#48), endpoints from #49 and #50. Work: write `development/test_sharing_deep_link.py` — end-to-end test: (1) generate a short Chagall tour (3 stops, `STORIED_MODE=true`), (2) call `POST /tour/share` with the tour text, (3) extract `share_url` from response, (4) call `GET /tour/{id}` using the tour_id, (5) assert returned `tour_text` matches original, (6) assert `share_count` incremented to 1 after retrieval, (7) call `GET /tour/{id}` again, assert `share_count` = 2. Output: test script committed. | `python test_sharing_deep_link.py` exits 0. All 7 assertions pass. Share URL resolves to correct tour text. share_count increments correctly on each retrieval. | P1 | 50, 49 | 1 hr | Week 4 |
| 76 | Storied | Development | Claude | Write `test_referral_flow.py` — verify referral code create → redeem → attribution | Input: `referral_engine.py` (#51), endpoints from #52. Work: write `development/test_referral_flow.py` — (1) create referral code for "user_001" via `POST /referral/create`, (2) assert code is 6-char alphanumeric, (3) redeem code as "new_user_001" via `POST /referral/redeem`, (4) assert response contains `referrer_user_id: "user_001"`, (5) redeem same code again as "new_user_002", (6) assert second redemption succeeds and redemption_count = 2, (7) attempt redeem with unknown code, assert 404. Output: test script committed. | `python test_referral_flow.py` exits 0. All 7 steps pass. referrer_user_id correctly attributed. redemption_count increments. Unknown code returns 404. | P2 | 52 | 1 hr | Week 4 |
| 77 | Storied | Development | Claude | Write `storied_db_migration.sql` — single SQL file for all new Storied tables | Input: `persona_preference_store.py` (#44), `tour_sharing.py` (#48), `referral_engine.py` (#51), `tour_cache_layer1.py` (#19). Work: write `development/storied_db_migration.sql` containing idempotent `CREATE TABLE IF NOT EXISTS` statements for all 5 new tables: `tour_cache`, `user_preferences`, `shared_tours`, `referral_codes`, `referral_redemptions`. Each statement includes all columns, types, constraints, and defaults defined in the individual modules. Add a comment header with version `-- Storied v2.2.0 migration` and a `SELECT 'storied_migration_complete' AS status;` at the end. Output: `storied_db_migration.sql` committed. | `docker exec -i development-postgres-2-1 psql -U admin audiotours < storied_db_migration.sql` exits 0 and prints `storied_migration_complete`. Running the same file a second time exits 0 (idempotent). All 5 tables visible in `\dt` output. | P1 | 44, 48, 51, 19 | 2 hrs | Week 4 |
| 78 | Storied | Development | Claude | Write `run_storied_db_migration.py` — migration runner with pre/post validation | Input: `storied_db_migration.sql` (#77), `development-postgres-2-1` container. Work: write `development/run_storied_db_migration.py` — connects to Postgres via `DATABASE_URL` env var, reads and executes `storied_db_migration.sql`, then validates all 5 tables exist with expected column counts (tour_cache: 4 cols, user_preferences: 3, shared_tours: 7, referral_codes: 4, referral_redemptions: 3). Prints MIGRATION OK or MIGRATION FAILED per table. Output: `run_storied_db_migration.py` committed. | `python run_storied_db_migration.py` exits 0 and prints `MIGRATION OK` for all 5 tables. Running twice exits 0 both times (idempotent). If `DATABASE_URL` missing: prints clear error and exits 1 without traceback. | P1 | 77 | 1 hr | Week 4 |
| 79 | Storied | Development | Claude | Final pre-release: set `STORIED_MODE=true` in docker-compose and run full integration test suite | Input: `docker-compose.yml` after #59, `integration_test_storied_full.py` (#66), `regression_all_tour_types.py` (#65), `content_qa_runner.py` (#39), `storied_smoke_test.py` (#60). Work: update docker-compose to set `STORIED_MODE=true` for tour-generator service (this is the Aug 1 production config). Rebuild container. Run in sequence: (1) `run_storied_db_migration.py`, (2) `storied_smoke_test.py`, (3) `regression_all_tour_types.py`, (4) `integration_test_storied_full.py`, (5) `content_qa_runner.py` on freshly generated Chagall tour. All must exit 0. Commit results summary as `storied_final_test_results.md`. Output: updated `docker-compose.yml` + `storied_final_test_results.md` committed. | All 5 scripts exit 0. `storied_final_test_results.md` exists with timestamp, all test names, and PASS status. `docker exec development-tour-generator-1 printenv STORIED_MODE` returns `true`. | P1 | 66, 65, 39, 60, 78 | 3 hrs | Week 4 |
| 80 | Storied | Development | Claude | Tag Storied release commit and produce `storied_handoff_for_mobile.md` | Input: `storied_final_test_results.md` (#79), `storied_launch_checklist.md` (#71), `storied_release_notes.md` (#70), `storied_rollback_plan.md` (#72). Work: (1) run `git tag storied-v2.2.0-services` on the `storied` branch HEAD after all tests pass; (2) write `development/storied_handoff_for_mobile.md` — handoff doc for Mobile Q and iOS Q: services version and tag, all new endpoint URLs + request/response contracts for `/user/persona`, `/tour/share`, `/tour/{id}`, `/referral/create`, `/referral/redeem`, `X-Attestation-Token` header spec, `persona` parameter on tour generation request, link to `storied_release_notes.md` and `storied_rollback_plan.md`. Output: tag created, `storied_handoff_for_mobile.md` committed and pushed. | `git tag` output includes `storied-v2.2.0-services`. `storied_handoff_for_mobile.md` exists and contains all 5 endpoint contracts with request/response examples. File references `storied_rollback_plan.md` and `storied_release_notes.md` by filename. | P1 | 79, 71, 70, 72 | 2 hrs | Week 4 |
