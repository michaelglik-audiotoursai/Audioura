# Audioura Release: Storied — Final Batch (Tasks 81–95)

| # | Release | Track | Owner | Task name | Description (inputs → work → outputs) | Acceptance criteria | Priority | Depends on (#) | Est. effort | Target week |
|---|---------|-------|-------|-----------|----------------------------------------|---------------------|----------|----------------|-------------|-------------|
| 81 | Storied | Development | Claude | Wire `persona` and `STORIED_MODE` through `tour_orchestrator_service.py` request path | Input: `tour_orchestrator_service.py` (container `development-tour-orchestrator-1:5002`), `generate_tour_text.py` after #43, `persona_preference_store.py` (#44). Work: in `tour_orchestrator_service.py`, extract optional `persona` field from the incoming mobile generate request body. Before calling `generate_tour_text()`, call `get_persona(user_id, db_url)` if `user_id` present (overrides request body persona if stored preference exists). Pass resolved `persona` to `generate_tour_text()`. Log `PERSONA_RESOLVED: {persona}` or `PERSONA_RESOLVED: none` on every request. Output: updated `tour_orchestrator_service.py` committed, copied to container with `docker cp`, container restarted. | `POST /generate` with body `{..., "persona": "art_lover"}` logs `PERSONA_RESOLVED: art_lover`. `POST /generate` with `user_id` that has stored "history_buff" logs `PERSONA_RESOLVED: history_buff` (stored preference wins). `POST /generate` with no persona field logs `PERSONA_RESOLVED: none`. All 3 cases complete tour generation without error. | P1 | 43, 46 | 2 hrs | Week 2 |
| 82 | Storied | Development | Claude | Wire `POST /tour/share` auto-call after successful tour generation in `tour_orchestrator_service.py` | Input: `tour_orchestrator_service.py` after #81, `tour_sharing.py` (#48), endpoint from #49. Work: after `generate_tour_text()` returns successfully and tour is stored in Postgres, when `STORIED_MODE=true`, automatically call `store_shared_tour()` with the generated tour text and metadata. Return `share_id` and `share_url` as additional fields in the orchestrator response JSON alongside the existing `final_tour_id`. When `STORIED_MODE=false`: response unchanged (no share fields). Failure to store share must never block the tour response — wrap in try/except, log warning only. Output: updated `tour_orchestrator_service.py` committed and deployed to container. | `POST /generate` with `STORIED_MODE=true` returns JSON containing `final_tour_id`, `share_id`, and `share_url`. `POST /generate` with `STORIED_MODE=false` returns only `final_tour_id` (unchanged). If `store_shared_tour()` raises exception: tour response still returns 200 with `final_tour_id`, logs `SHARE_STORE_WARN`. | P1 | 82, 49 | 2 hrs | Week 3 |
| 83 | Storied | Development | Claude | Add deep-link resolution endpoint `GET /resolve/tour/{share_id}` to `tour-id-resolution-1` service | Input: `tour_sharing.py` (#48), `tour-id-resolution-1:5025` service file, `remind_Services_ai.md` service map. Work: add `GET /resolve/tour/<share_id>` to the tour-id-resolution service — calls `get_shared_tour(share_id, db_url)`, returns 200 `{tour_id, location, tour_type, total_stops, share_url}` for mobile deep-link handling, or 404 `{error: "not found"}`. No API key required (same as existing open read endpoints). Increments `share_count`. Output: updated resolution service file committed and deployed to container with `docker cp` + restart. | `GET /resolve/tour/{valid_share_id}` returns 200 with `tour_id` field. `GET /resolve/tour/nonexistent` returns 404. No API key needed. `share_count` increments on each call. Response arrives in < 500ms. | P1 | 50, 48 | 2 hrs | Week 3 |
| 84 | Storied | Development | Claude | Write `test_orchestrator_storied_wiring.py` — verify orchestrator passes all Storied params correctly | Input: `tour_orchestrator_service.py` after #82, `test_persona_weighted_tour.py` (#74). Work: write `development/test_orchestrator_storied_wiring.py` — hits the running `development-tour-orchestrator-1:5002` with 4 requests: (1) `STORIED_MODE=true` + persona="art_lover" → assert response contains `share_id` and `share_url`, assert logs contain `PERSONA_RESOLVED: art_lover`; (2) `STORIED_MODE=true` + no persona → assert response contains `share_id`, logs contain `PERSONA_RESOLVED: none`; (3) `STORIED_MODE=false` → assert response does NOT contain `share_id`; (4) `STORIED_MODE=true` + stored persona for user_id → assert stored preference used. Prints PASS/FAIL per case. Output: script committed. | `python test_orchestrator_storied_wiring.py` exits 0 all 4 cases PASS. | P1 | 82, 81 | 2 hrs | Week 3 |
| 85 | Storied | Development | Claude | Add `storied` column to `audio_tours` DB table and wire tour lineage | Input: `remind_Services_ai.md` `audio_tours` schema, `storied_db_migration.sql` (#77), `tour_orchestrator_service.py` after #82. Work: add to `storied_db_migration.sql` a new `ALTER TABLE audio_tours ADD COLUMN IF NOT EXISTS storied_mode BOOLEAN DEFAULT FALSE` statement. In `tour_orchestrator_service.py`, when storing the generated tour in `audio_tours`, set `storied_mode = (STORIED_MODE env var == 'true')`. This lets the app distinguish Storied tours from Beta tours in the DB for future analytics. Output: updated `storied_db_migration.sql` and `tour_orchestrator_service.py` committed. | `ALTER TABLE` runs idempotently (IF NOT EXISTS). `docker exec development-postgres-2-1 psql -U admin audiotours -c "\d audio_tours"` shows `storied_mode` column. Tours generated with `STORIED_MODE=true` have `storied_mode=true` in DB. Tours generated with `STORIED_MODE=false` have `storied_mode=false`. | P2 | 77, 82 | 1 hr | Week 3 |
| 86 | Storied | Development | Claude | Write `storied_merge_forward_procedure.md` — how to merge Beta bug fixes into storied branch | Input: `git_branch_strategy.md`, `STORIED_DEVELOPMENT_PLAN.md` ("Bug-fix rule: Beta bugs fixed on main, re-tagged, then merged forward into storied"). Work: write `development/storied_merge_forward_procedure.md` with exact git commands for the merge-forward workflow: (1) fix lands on `main` and is tagged; (2) `git checkout storied`; (3) `git merge main --no-ff -m "Merge Beta fix vX.X.X into storied"`; (4) resolve conflicts (conflict zones identified: `generate_tour_text.py` Phase 5 area, `tour_orchestrator_service.py` request handler); (5) run `regression_all_tour_types.py` to confirm Beta parity still holds after merge; (6) push. Include a conflict resolution guide specific to the 2 known conflict zones. Output: `storied_merge_forward_procedure.md` committed. | File exists. Contains all 6 steps with exact git commands. Conflict resolution section names `generate_tour_text.py` and `tour_orchestrator_service.py` specifically. Step 5 references `regression_all_tour_types.py` by filename. Doc is followable by Sir Michael without developer assistance. | P1 | 65 | 2 hrs | Week 3 |
| 87 | Storied | Development | Claude | Update `remind_advisor.md` with full Storied branch status and task index | Input: `remind_advisor.md`, all 4 task batch files (#1–#80 + this batch). Work: add a new section `## 🎨 STORIED BRANCH STATUS` to `remind_advisor.md` covering: current storied branch HEAD commit, `STORIED_MODE` flag status, all 5 feature areas and their task number ranges (spine: 1–20, de-repetition/directions: 21–40, personalization: 41–46, sharing/referral: 47–52, attestation: 53–58), key new files created (list all new .py files), Aug 1 target, link to `storied_launch_checklist.md`. Update the IMMEDIATE NEXT ACTIONS section to reflect Storied as active Track B work. Output: updated `remind_advisor.md` committed to `storied` branch. | `remind_advisor.md` contains `## 🎨 STORIED BRANCH STATUS` section. All 5 feature areas listed with task ranges. New Python files listed. `storied_launch_checklist.md` referenced by filename. IMMEDIATE NEXT ACTIONS section updated. | P1 | 71, 80 | 1 hr | Week 4 |
| 88 | Storied | Development | Claude | Update `remind_Services_ai.md` with Storied branch context and new service files | Input: `remind_Services_ai.md`, all new service files created in tasks #3, #5, #7, #30, #41, #44, #47, #51, #53. Work: add a `## 🎨 STORIED MODE` section to `remind_Services_ai.md` covering: (a) `STORIED_MODE` env var and what it gates, (b) list of all new Python modules with one-line descriptions, (c) new endpoints added (`/user/persona`, `/tour/share`, `/tour/{id}`, `/referral/create`, `/referral/redeem`, `/resolve/tour/{id}`), (d) new DB tables (5 total), (e) recovery note: "On storied branch recovery, run `run_storied_db_migration.py` before starting containers". Output: updated `remind_Services_ai.md` committed to `storied` branch. | Section `## 🎨 STORIED MODE` present. All 6 new endpoints listed with HTTP method and port. All 5 new DB tables listed. Recovery note present. File remains valid Markdown (no broken headers). | P1 | 78, 83 | 1 hr | Week 4 |
| 89 | Storied | Development | Claude | Write `storied_handoff_for_ios.md` — iOS-specific Storied integration contract | Input: `storied_handoff_for_mobile.md` (#80), `transition_for_iOS_AQ.md`, `STORIED_DEVELOPMENT_PLAN.md`. Work: write `development/storied_handoff_for_ios.md` — iOS-specific counterpart to the Mobile handoff: (a) `X-Attestation-Token` header — iOS sends App Attest token, platform header value `"ios"`; (b) persona endpoint contracts (same as Android but note Swift URLSession example vs Dart); (c) share URL format and how to open it as a deep link in the iOS app; (d) `storied_mode` field in tour list response (for future "Storied" badge UI); (e) note that `ATTESTATION_MODE=log_only` means iOS will never be blocked by attestation in Aug 1 build. Output: `storied_handoff_for_ios.md` committed. | File exists. All 5 sections present. App Attest header spec matches `attestation_verifier.py` (#55) parameter names. Share URL format matches `build_share_url()` output. Log-only note explicitly states no iOS request will be blocked. | P1 | 80, 55 | 1 hr | Week 4 |
| 90 | Storied | Development | Claude | Write `load_test_storied_pipeline.py` — concurrent request stress test for Storied generation | Input: `generate_tour_text.py` with `STORIED_MODE=true`, `development-tour-generator-1:5000`, Python `concurrent.futures`. Work: write `development/load_test_storied_pipeline.py` — fires 5 concurrent tour generation requests (3-stop tours, `STORIED_MODE=true`, different locations) using ThreadPoolExecutor, measures: wall-clock time per request, success/failure count, any race conditions (duplicate spine generation, cache collision). Reports: all 5 completion times, pass/fail, max cost across 5 runs. Output: script committed. | `python load_test_storied_pipeline.py` completes all 5 concurrent requests without exception. All 5 return valid tour text. No two tours share identical spine (race condition check). Max single-tour cost < $0.15. All 5 complete within 180s wall clock. | P2 | 13, 20 | 2 hrs | Week 4 |
| 91 | Storied | Development | Claude | Write `storied_feature_flags.md` — master reference for all Storied env vars and safe values | Input: all env vars introduced across tasks #12, #56, #59, #68. Work: write `development/storied_feature_flags.md` — a single reference table of every env var introduced by the Storied release: `STORIED_MODE` (false/true, default false), `ATTESTATION_MODE` (off/log_only/enforce, default log_only), `BASE_URL` (share URL base), `REFERRAL_BASE_URL`, `DATABASE_URL` (required for cache + persona + sharing). For each: safe default, what breaks if missing, which containers need it, Aug-1 recommended value. Also include a "minimum viable Storied config" block showing the exact docker-compose environment snippet to paste for Aug 1 readiness. Output: `storied_feature_flags.md` committed. | File exists. All 5 env vars documented. Each has: safe default, consequence-if-missing, container list. "Minimum viable Storied config" block is copy-pasteable into docker-compose and activates all Storied features except enforce mode. | P1 | 59, 69 | 1 hr | Week 4 |
| 92 | Storied | Development | Claude | Write `storied_tasks_index.md` — master index of all 95 tasks with status tracking | Input: all 5 task batch files (`first_storied_20_tasks.md`, `storied_tasks_21_40.md`, `storied_tasks_41_60.md`, `storied_tasks_61_80.md`, this batch). Work: write `development/storied_tasks_index.md` — a single-table index of all tasks: task #, task name, output file/artifact, owner, target week, status column (blank — for Claude.AI to fill in ClickUp). Group by feature area: Spine & Content (1–20), De-repetition & Directions (21–40), Personalization (41–46), Sharing & Referral (47–52), Attestation (53–58), Integration & QA (59–80), Orchestrator & Operations (81–95). Include a summary row per group showing task count and total estimated effort. Output: `storied_tasks_index.md` committed. | File exists. All 95 tasks present (verified by row count). All 7 feature group sections present. Each row has task #, name, and output artifact. Summary rows show correct task counts per group. File is valid Markdown table parseable by Claude.AI for ClickUp import. | P1 | — | 2 hrs | Week 4 |
| 93 | Storied | Development | Claude | Write `storied_demo_script.md` — step-by-step demo script for Storied tester onboarding | Input: `storied_release_notes.md` (#70), `storied_handoff_for_mobile.md` (#80), `mobile_demo_recording_guide.md`. Work: write `development/storied_demo_script.md` — a walkthrough script a tester follows to experience all 5 Storied features in sequence: (1) open app → answer onboarding question → note persona selection, (2) generate a museum tour → listen to hook intro, (3) notice distinct emotional beats per stop (compare Stop 1 vs Stop 4), (4) share the tour → verify link opens on another device, (5) invite a friend via referral → verify attribution. For each step: what to tap, what to expect, what "good" looks like. Output: `storied_demo_script.md` committed. | File exists. All 5 feature steps present. Each step has: tap target, expected UI response, "what good looks like" sentence. Total reading time < 5 minutes (tester-friendly). No step requires developer knowledge or CLI access. | P2 | 70, 80 | 1 hr | Week 4 |
| 94 | Storied | Development | Claude | Run `storied_db_migration.sql` against production Postgres and verify all 5 tables | Input: `storied_db_migration.sql` (#77), `run_storied_db_migration.py` (#78), production `DATABASE_URL` (from Secret Manager or env). Work: run `run_storied_db_migration.py` against the production Cloud SQL instance (not just local Docker). Verify all 5 tables exist in production DB. Document result in `storied_db_migration_prod_log.md` with: timestamp, tables created, any warnings, confirming entry `SELECT 'storied_migration_complete'` output. Output: `storied_db_migration_prod_log.md` committed. | `storied_db_migration_prod_log.md` exists. Contains timestamp. Shows all 5 tables as MIGRATION OK. No ERROR lines. Production `audio_tours` table still has all existing rows (migration did not touch existing data — only IF NOT EXISTS creates). | P1 | 78, 79 | 1 hr | Week 4 |
| 95 | Storied | Development | Claude | Final gate: run `storied_launch_checklist.md` and mark all automated items PASS before handoff to Mobile | Input: `storied_launch_checklist.md` (#71), all test scripts (#66, #65, #39, #60, #84, #90), `storied_final_test_results.md` (#79). Work: execute every automated checklist item in `storied_launch_checklist.md` in sequence. For each automated item: run the referenced script, record exit code, mark `[x]` PASS or `[ ]` FAIL in a copy saved as `storied_checklist_results.md`. For Michael-owned items (privacy policy, Data Safety, App Privacy, demo account, keystore backup): leave as `[ ] PENDING — owner: Michael`. Commit `storied_checklist_results.md`. This is the final Services-side deliverable before Mobile Q and iOS Q begin their Storied UI work. Output: `storied_checklist_results.md` committed, pushed, `storied-v2.2.0-services-final` git tag created. | `storied_checklist_results.md` exists. All automated items marked `[x] PASS`. All Michael-owned items marked `PENDING` with owner noted. `git tag` shows `storied-v2.2.0-services-final`. File pushed to `origin/storied`. This commit is the handoff point for Mobile Q and iOS Q. | P1 | 71, 84, 90, 94 | 2 hrs | Week 4 |
